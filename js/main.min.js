"use strict";

var valueText = document.querySelector(".orderform__form__slider__value"),
  sliderInput = document.querySelector(".orderform__form__slider__range");
function updateSliderValue() {
  valueText.innerHTML && (valueText.innerHTML = sliderInput.value + "%");
}